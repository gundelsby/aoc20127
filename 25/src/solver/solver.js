const Tape = require('./tape')
const {Cursor} = require('./cursor')
const states = require('./states')

module.exports = {
  part1: () => {
    // Begin in state A.
    // Perform a diagnostic checksum after 12919244 steps.
    const cursor = new Cursor(new Tape())

  },
  part2: (input) => {
    return null
  }
}
